commit 1f45d7bd58740d5cdb9d06711b7d959f2a84ecfd
Author: AI Bot <bot@example.com>
Date:   Thu Aug 6 14:33:15 2026 -0500

    Fix modal and card dark backgrounds

diff --git a/admin/css/admin.css b/admin/css/admin.css
index 6626bdc..841cef0 100644
--- a/admin/css/admin.css
+++ b/admin/css/admin.css
@@ -39,22 +39,6 @@ body {
     line-height: 1.6;
 }
 
-/* ── Override global-theme.css Backgrounds for Light Mode ── */
-.gradient-bg {
-    position: fixed; inset: 0; z-index: -2;
-    background: radial-gradient(circle at 20% 30%, var(--primary-glow) 0%, transparent 40%),
-                radial-gradient(circle at 80% 70%, var(--primary-glow) 0%, transparent 45%),
-                var(--bg);
-}
-
-.grid-pattern {
-    position: fixed; inset: 0; z-index: -1; pointer-events: none;
-    background-image: 
-        linear-gradient(rgba(0, 0, 0, 0.03) 1px, transparent 1px),
-        linear-gradient(90deg, rgba(0, 0, 0, 0.03) 1px, transparent 1px);
-    background-size: 60px 60px;
-}
-
 body.tour-open {
     touch-action: none;
     overscroll-behavior: contain;
@@ -75,8 +59,9 @@ button { cursor: pointer; }
 .sidebar {
     width: var(--sidebar-w);
     background: var(--surface);
+    backdrop-filter: blur(20px);
+    -webkit-backdrop-filter: blur(20px);
     border-right: 1px solid var(--border);
-    box-shadow: 2px 0 8px rgba(0,0,0,0.02);
     display: flex;
     flex-direction: column;
     position: fixed;
@@ -102,7 +87,7 @@ button { cursor: pointer; }
     justify-content: center;
     font-weight: 700;
     font-size: 13px;
-    color: #000;
+    color: white;
     flex-shrink: 0;
 }
 
@@ -217,7 +202,7 @@ button.nav-item {
     gap: 6px;
 }
 
-.btn-primary   { background: var(--primary); color: #000; }
+.btn-primary   { background: var(--primary); color: white; }
 .btn-primary:hover { background: var(--primary-dim); transform: translateY(-1px); }
 
 .btn-secondary { background: var(--surface-2); color: var(--text); border: 1px solid var(--border); }
@@ -244,9 +229,10 @@ button.nav-item {
 
 .stat-card {
     background: var(--surface);
+    backdrop-filter: blur(10px);
+    -webkit-backdrop-filter: blur(10px);
     border: 1px solid var(--border);
     border-radius: var(--radius);
-    box-shadow: var(--shadow-sm);
     padding: 20px;
     display: flex;
     align-items: center;
@@ -278,9 +264,10 @@ button.nav-item {
 /* ── Plan Card ──────────────────────────────────────────────────────────────── */
 .plan-card {
     background: var(--surface);
+    backdrop-filter: blur(10px);
+    -webkit-backdrop-filter: blur(10px);
     border: 1px solid var(--border);
     border-radius: var(--radius);
-    box-shadow: var(--shadow-sm);
     padding: 24px;
     margin-bottom: 24px;
 }
@@ -296,7 +283,7 @@ button.nav-item {
     gap: 6px;
     padding: 12px 14px;
     border-radius: 16px;
-    background: rgba(255, 255, 255, 0.03);
+    background: var(--surface-3);
     border: 1px solid rgba(255, 255, 255, 0.06);
 }
 .usage-label { font-size: 12px; color: var(--text-dim); }
@@ -326,9 +313,10 @@ button.nav-item {
 
 .action-card {
     background: var(--surface);
+    backdrop-filter: blur(10px);
+    -webkit-backdrop-filter: blur(10px);
     border: 1px solid var(--border);
     border-radius: var(--radius);
-    box-shadow: var(--shadow-sm);
     padding: 20px 16px;
     text-align: center;
     transition: all var(--transition);
@@ -384,7 +372,7 @@ button.nav-item {
     padding: 12px 14px;
     border-radius: 14px;
     border: 1px solid var(--border);
-    background: rgba(255, 255, 255, 0.03);
+    background: var(--surface-3);
     color: var(--text);
     font-size: 13px;
 }
@@ -477,7 +465,7 @@ button.nav-item {
     padding: 22px;
     border-radius: 24px;
     border: 1px solid rgba(165, 180, 252, 0.24);
-    background: linear-gradient(180deg, rgba(14, 18, 32, 0.96), rgba(15, 18, 30, 0.88));
+    background: var(--surface);
     box-shadow:
         0 32px 80px rgba(2, 6, 23, 0.55),
         0 0 0 1px rgba(99, 102, 241, 0.16),
@@ -550,7 +538,7 @@ button.nav-item {
     height: 8px;
     border-radius: 999px;
     overflow: hidden;
-    background: rgba(255, 255, 255, 0.08);
+    background: var(--surface-3);
     border: 1px solid rgba(255, 255, 255, 0.04);
 }
 
@@ -572,7 +560,7 @@ button.nav-item {
     padding: 0 12px;
     border-radius: 999px;
     color: #bfdbfe;
-    background: rgba(30, 41, 59, 0.58);
+    background: rgba(0,0,0,0.4);
     border: 1px solid rgba(129, 140, 248, 0.22);
     font-size: 0.76rem;
     font-weight: 600;
@@ -707,8 +695,8 @@ button.nav-item {
         gap: 10px;
         background: linear-gradient(
             180deg,
-            rgba(14, 18, 32, 0),
-            rgba(14, 18, 32, 0.96) 20%
+            rgba(255,255,255,0),
+            var(--surface) 20%
         );
         padding-top: 10px;
         padding-bottom: calc(env(safe-area-inset-bottom, 0px) + 2px);
@@ -768,9 +756,10 @@ button.nav-item {
 /* ── Table ──────────────────────────────────────────────────────────────────── */
 .table-wrap {
     background: var(--surface);
+    backdrop-filter: blur(10px);
+    -webkit-backdrop-filter: blur(10px);
     border: 1px solid var(--border);
     border-radius: var(--radius);
-    box-shadow: var(--shadow-sm);
     overflow: hidden;
 }
 
@@ -849,7 +838,7 @@ tbody tr:hover td { background: var(--surface-2); }
 .settings-card {
     position: relative;
     overflow: hidden;
-    background: linear-gradient(180deg, rgba(17, 20, 32, 0.88), rgba(17, 20, 32, 0.72));
+    background: var(--surface-2);
     border: 1px solid rgba(255, 255, 255, 0.09);
     box-shadow: 0 24px 60px rgba(6, 10, 22, 0.22);
 }
@@ -916,7 +905,7 @@ tbody tr:hover td { background: var(--surface-2); }
     padding: 14px 16px;
     border-radius: 14px;
     border: 1px solid var(--border);
-    background: rgba(255, 255, 255, 0.03);
+    background: var(--surface-3);
 }
 
 .settings-inline-row,
@@ -928,7 +917,7 @@ tbody tr:hover td { background: var(--surface-2); }
 }
 
 .branding-preview {
-    background: linear-gradient(135deg, rgba(255, 255, 255, 0.04), rgba(255, 255, 255, 0.02));
+    background: var(--surface-3);
     border: 1px solid var(--border);
     border-radius: 16px;
     padding: 14px 16px;
@@ -986,7 +975,7 @@ tbody tr:hover td { background: var(--surface-2); }
     width: 100%;
     padding: 18px;
     text-align: left;
-    background: rgba(255, 255, 255, 0.03);
+    background: var(--surface-3);
     border: 1px solid var(--border);
     border-radius: 16px;
     color: var(--text);
@@ -1018,7 +1007,7 @@ tbody tr:hover td { background: var(--surface-2); }
     padding: 18px;
     border-radius: 16px;
     border: 1px solid var(--border);
-    background: rgba(255, 255, 255, 0.03);
+    background: var(--surface-3);
 }
 
 .calendar-subscribe-help-title {
@@ -1125,7 +1114,7 @@ tbody tr:hover td { background: var(--surface-2); }
     border-radius: 999px;
     font-size: 12px;
     color: var(--text-dim);
-    background: rgba(255, 255, 255, 0.03);
+    background: var(--surface-3);
     border: 1px solid var(--border);
 }
 
@@ -1156,7 +1145,7 @@ tbody tr:hover td { background: var(--surface-2); }
     gap: 14px;
     padding: 18px;
     background: linear-gradient(135deg, rgba(255, 255, 255, 0.045), rgba(255, 255, 255, 0.02));
-    border: 1px solid rgba(255, 255, 255, 0.08);
+    border: 1px solid var(--border);
     border-radius: 18px;
     transition: border-color var(--transition), transform var(--transition), background var(--transition);
 }
@@ -1281,8 +1270,8 @@ tbody tr:hover td { background: var(--surface-2); }
     align-items: center;
     gap: 12px;
     min-height: 58px;
-    background: rgba(15, 19, 31, 0.82);
-    border: 1px solid rgba(255, 255, 255, 0.08);
+    background: var(--surface);
+    border: 1px solid var(--border);
     border-radius: 14px;
     padding: 8px 10px 8px 14px;
 }
@@ -1422,7 +1411,7 @@ select option { background: var(--surface-2); }
     justify-content: center;
     font-size: 20px;
     font-weight: 800;
-    color: #000;
+    color: white;
     margin-bottom: 12px;
 }
 
@@ -1435,7 +1424,7 @@ select option { background: var(--surface-2); }
     width: 100%;
     padding: 12px;
     background: var(--primary);
-    color: #000;
+    color: white;
     border: none;
     border-radius: var(--radius-sm);
     font-size: 14px;
@@ -1838,7 +1827,7 @@ select option { background: var(--surface-2); }
 .modal-overlay.open { display: flex; }
 
 .modal {
-    background: rgba(15, 19, 31, 0.95);
+    background: var(--surface);
     backdrop-filter: blur(24px);
     -webkit-backdrop-filter: blur(24px);
     border: 1px solid var(--border);
@@ -1879,6 +1868,22 @@ select option { background: var(--surface-2); }
     input, select, textarea, .form-control { font-size: 16px !important; min-height: 44px; }
     .modal-overlay { align-items: flex-end; padding: 0; }
     .modal { width: 100%; max-width: none; max-height: calc(100dvh - env(safe-area-inset-top)); margin: 0; padding: 22px 18px calc(18px + env(safe-area-inset-bottom)); border-radius: 22px 22px 0 0; }
-    .modal-footer { position: sticky; bottom: calc(-18px - env(safe-area-inset-bottom)); flex-direction: column; padding: 12px 0 calc(12px + env(safe-area-inset-bottom)); background: rgba(15, 19, 31, .98); }
+    .modal-footer { position: sticky; bottom: calc(-18px - env(safe-area-inset-bottom)); flex-direction: column; padding: 12px 0 calc(12px + env(safe-area-inset-bottom)); background: var(--surface); }
+    .modal-footer > * { width: 100%; }
+}
+
+/* Final mobile overrides intentionally live after the base modal rules. */
+.availability-layout { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); gap: 24px; align-items: start; }
+@media (max-width: 700px) {
+    html, body { min-height: 100dvh; }
+    .sidebar { height: 100dvh; padding-top: env(safe-area-inset-top); padding-bottom: env(safe-area-inset-bottom); }
+    .hamburger { top: calc(14px + env(safe-area-inset-top)); left: calc(14px + env(safe-area-inset-left)); min-width: 44px; min-height: 44px; }
+    .main-content { min-height: 100dvh; padding-top: calc(72px + env(safe-area-inset-top)); padding-right: max(16px, env(safe-area-inset-right)); padding-bottom: calc(24px + env(safe-area-inset-bottom)); padding-left: max(16px, env(safe-area-inset-left)); }
+    .availability-layout { grid-template-columns: 1fr; gap: 16px; }
+    button, .btn, .nav-item, .action-btn, .modal-close { min-height: 44px; }
+    input, select, textarea, .form-control { font-size: 16px !important; min-height: 44px; }
+    .modal-overlay { align-items: flex-end; padding: 0; }
+    .modal { width: 100%; max-width: none; max-height: calc(100dvh - env(safe-area-inset-top)); margin: 0; padding: 22px 18px calc(18px + env(safe-area-inset-bottom)); border-radius: 22px 22px 0 0; }
+    .modal-footer { position: sticky; bottom: calc(-18px - env(safe-area-inset-bottom)); flex-direction: column; padding: 12px 0 calc(12px + env(safe-area-inset-bottom)); background: var(--surface); }
     .modal-footer > * { width: 100%; }
 }

commit d9802870297fc40b62597e61bb2aa22aecf76e54
Author: AI Bot <bot@example.com>
Date:   Thu Aug 6 14:28:18 2026 -0500

    Fix light theme overrides for global backgrounds and inline text

diff --git a/admin/css/admin.css b/admin/css/admin.css
index 2f57713..6626bdc 100644
--- a/admin/css/admin.css
+++ b/admin/css/admin.css
@@ -39,6 +39,22 @@ body {
     line-height: 1.6;
 }
 
+/* ── Override global-theme.css Backgrounds for Light Mode ── */
+.gradient-bg {
+    position: fixed; inset: 0; z-index: -2;
+    background: radial-gradient(circle at 20% 30%, var(--primary-glow) 0%, transparent 40%),
+                radial-gradient(circle at 80% 70%, var(--primary-glow) 0%, transparent 45%),
+                var(--bg);
+}
+
+.grid-pattern {
+    position: fixed; inset: 0; z-index: -1; pointer-events: none;
+    background-image: 
+        linear-gradient(rgba(0, 0, 0, 0.03) 1px, transparent 1px),
+        linear-gradient(90deg, rgba(0, 0, 0, 0.03) 1px, transparent 1px);
+    background-size: 60px 60px;
+}
+
 body.tour-open {
     touch-action: none;
     overscroll-behavior: contain;

commit 63b61f60a12cdcf81bfa1ce9f5337d870b8054de
Author: AI Bot <bot@example.com>
Date:   Thu Aug 6 14:24:42 2026 -0500

    Admin Dashboard Mega Redesign to White/Purple Theme

diff --git a/admin/css/admin.css b/admin/css/admin.css
index cfaa987..2f57713 100644
--- a/admin/css/admin.css
+++ b/admin/css/admin.css
@@ -6,27 +6,29 @@
 *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
 
 :root {
-    --gold:       #6366f1;
-    --gold-dim:   #8b5cf6;
-    --gold-glow:  rgba(99, 102, 241, 0.15);
-    --bg:         transparent;
-    --surface:    rgba(20, 20, 30, 0.5);
-    --surface-2:  rgba(255, 255, 255, 0.03);
-    --surface-3:  rgba(255, 255, 255, 0.06);
-    --border:     rgba(255, 255, 255, 0.08);
-    --text:       #e8e8ee;
-    --text-dim:   #8888aa;
-    --success:    #22c55e;
-    --danger:     #ef4444;
-    --warning:    #f59e0b;
-    --info:       #3b82f6;
+    --primary:    #7e22ce;
+    --primary-dim: #9333ea;
+    --primary-glow: rgba(126, 34, 206, 0.15);
+    --bg:         #f8fafc;
+    --surface:    #ffffff;
+    --surface-2:  #f1f5f9;
+    --surface-3:  #e2e8f0;
+    --border:     #e2e8f0;
+    --text:       #0f172a;
+    --text-dim:   #64748b;
+    --success:    #16a34a;
+    --danger:     #dc2626;
+    --warning:    #d97706;
+    --info:       #2563eb;
     --sidebar-w:  240px;
     --radius:     12px;
     --radius-sm:  8px;
     --transition: 200ms ease;
+    --shadow-sm:  0 1px 2px 0 rgba(0, 0, 0, 0.05);
+    --shadow-md:  0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
 }
 
-html { font-size: 15px; }
+html { font-size: 15px; background-color: var(--bg); overscroll-behavior-y: none; }
 
 body {
     font-family: 'Satoshi', sans-serif;
@@ -57,9 +59,8 @@ button { cursor: pointer; }
 .sidebar {
     width: var(--sidebar-w);
     background: var(--surface);
-    backdrop-filter: blur(20px);
-    -webkit-backdrop-filter: blur(20px);
     border-right: 1px solid var(--border);
+    box-shadow: 2px 0 8px rgba(0,0,0,0.02);
     display: flex;
     flex-direction: column;
     position: fixed;
@@ -78,7 +79,7 @@ button { cursor: pointer; }
 
 .brand-logo {
     width: 40px; height: 40px;
-    background: linear-gradient(135deg, var(--gold), var(--gold-dim));
+    background: linear-gradient(135deg, var(--primary), var(--primary-dim));
     border-radius: var(--radius-sm);
     display: flex;
     align-items: center;
@@ -90,7 +91,7 @@ button { cursor: pointer; }
 }
 
 .brand-name  { font-size: 13px; font-weight: 600; display: block; }
-.brand-plan  { font-size: 10px; color: var(--gold); letter-spacing: 1.5px; text-transform: uppercase; display: block; }
+.brand-plan  { font-size: 10px; color: var(--primary); letter-spacing: 1.5px; text-transform: uppercase; display: block; }
 
 .sidebar-nav {
     flex: 1;
@@ -120,7 +121,7 @@ button.nav-item {
 }
 
 .nav-item:hover { background: var(--surface-2); color: var(--text); }
-.nav-item.active { background: var(--gold-glow); color: var(--gold); font-weight: 500; }
+.nav-item.active { background: var(--primary-glow); color: var(--primary); font-weight: 500; }
 
 .nav-icon { font-size: 16px; width: 20px; text-align: center; }
 
@@ -146,7 +147,7 @@ button.nav-item {
     justify-content: center;
     font-weight: 600;
     font-size: 14px;
-    color: var(--gold);
+    color: var(--primary);
     flex-shrink: 0;
 }
 
@@ -200,8 +201,8 @@ button.nav-item {
     gap: 6px;
 }
 
-.btn-primary   { background: var(--gold); color: #000; }
-.btn-primary:hover { background: var(--gold-dim); transform: translateY(-1px); }
+.btn-primary   { background: var(--primary); color: #000; }
+.btn-primary:hover { background: var(--primary-dim); transform: translateY(-1px); }
 
 .btn-secondary { background: var(--surface-2); color: var(--text); border: 1px solid var(--border); }
 .btn-secondary:hover { background: var(--surface-3); }
@@ -227,10 +228,9 @@ button.nav-item {
 
 .stat-card {
     background: var(--surface);
-    backdrop-filter: blur(10px);
-    -webkit-backdrop-filter: blur(10px);
     border: 1px solid var(--border);
     border-radius: var(--radius);
+    box-shadow: var(--shadow-sm);
     padding: 20px;
     display: flex;
     align-items: center;
@@ -238,7 +238,7 @@ button.nav-item {
     transition: border-color var(--transition);
 }
 
-.stat-card:hover { border-color: var(--gold); }
+.stat-card:hover { border-color: var(--primary); }
 
 .stat-icon {
     width: 48px; height: 48px;
@@ -252,7 +252,7 @@ button.nav-item {
 }
 
 .confirmed-icon { background: rgba(34,197,94,0.1); }
-.monthly-icon   { background: var(--gold-glow); }
+.monthly-icon   { background: var(--primary-glow); }
 .designs-icon   { background: rgba(59,130,246,0.1); }
 .cancelled-icon { background: rgba(239,68,68,0.1); }
 
@@ -262,17 +262,16 @@ button.nav-item {
 /* ── Plan Card ──────────────────────────────────────────────────────────────── */
 .plan-card {
     background: var(--surface);
-    backdrop-filter: blur(10px);
-    -webkit-backdrop-filter: blur(10px);
     border: 1px solid var(--border);
     border-radius: var(--radius);
+    box-shadow: var(--shadow-sm);
     padding: 24px;
     margin-bottom: 24px;
 }
 
 .plan-header { display: flex; justify-content: space-between; align-items: center; gap: 24px; flex-wrap: wrap; }
 .plan-title  { font-size: 14px; color: var(--text-dim); font-weight: 400; margin-bottom: 4px; }
-.plan-name   { font-size: 20px; font-weight: 600; color: var(--gold); }
+.plan-name   { font-size: 20px; font-weight: 600; color: var(--primary); }
 
 .plan-usage { flex: 1; max-width: 320px; }
 .usage-bar-wrap {
@@ -293,7 +292,7 @@ button.nav-item {
 }
 .usage-fill {
     height: 100%;
-    background: linear-gradient(90deg, var(--gold), var(--gold-dim));
+    background: linear-gradient(90deg, var(--primary), var(--primary-dim));
     border-radius: 100px;
     transition: width 0.6s ease;
 }
@@ -311,10 +310,9 @@ button.nav-item {
 
 .action-card {
     background: var(--surface);
-    backdrop-filter: blur(10px);
-    -webkit-backdrop-filter: blur(10px);
     border: 1px solid var(--border);
     border-radius: var(--radius);
+    box-shadow: var(--shadow-sm);
     padding: 20px 16px;
     text-align: center;
     transition: all var(--transition);
@@ -322,8 +320,8 @@ button.nav-item {
 }
 
 .action-card:hover {
-    border-color: var(--gold);
-    background: var(--gold-glow);
+    border-color: var(--primary);
+    background: var(--primary-glow);
     transform: translateY(-2px);
 }
 
@@ -754,10 +752,9 @@ button.nav-item {
 /* ── Table ──────────────────────────────────────────────────────────────────── */
 .table-wrap {
     background: var(--surface);
-    backdrop-filter: blur(10px);
-    -webkit-backdrop-filter: blur(10px);
     border: 1px solid var(--border);
     border-radius: var(--radius);
+    box-shadow: var(--shadow-sm);
     overflow: hidden;
 }
 
@@ -807,7 +804,7 @@ tbody tr:hover td { background: var(--surface-2); }
 .badge-completed { background: rgba(99,102,241,0.15); color: #818cf8; }
 .badge-noshow    { background: rgba(245,158,11,0.15); color: #f59e0b; }
 .badge-new       { background: rgba(59,130,246,0.15); color: #60a5fa; }
-.badge-reviewed  { background: var(--gold-glow);      color: var(--gold); }
+.badge-reviewed  { background: var(--primary-glow);      color: var(--primary); }
 .badge-progress  { background: rgba(244,114,182,0.15); color: #f472b6; }
 .badge-pending   { background: rgba(156,163,175,0.1); color: #9ca3af; }
 .badge-default   { background: var(--surface-2);       color: var(--text-dim); }
@@ -1350,7 +1347,7 @@ input, select, textarea {
     outline: none;
 }
 
-input:focus, select:focus, textarea:focus { border-color: var(--gold); }
+input:focus, select:focus, textarea:focus { border-color: var(--primary); }
 input::placeholder, textarea::placeholder { color: var(--text-dim); }
 select option { background: var(--surface-2); }
 
@@ -1374,7 +1371,7 @@ select option { background: var(--surface-2); }
 .toast-show { opacity: 1; transform: translateY(0); }
 .toast-success { background: var(--surface); border: 1px solid var(--success); color: var(--success); }
 .toast-error   { background: var(--surface); border: 1px solid var(--danger);  color: var(--danger); }
-.toast-info    { background: var(--surface); border: 1px solid var(--gold);    color: var(--gold); }
+.toast-info    { background: var(--surface); border: 1px solid var(--primary);    color: var(--primary); }
 
 /* ── Login Page ─────────────────────────────────────────────────────────────── */
 .login-page {
@@ -1402,7 +1399,7 @@ select option { background: var(--surface-2); }
 
 .login-logo-mark {
     width: 56px; height: 56px;
-    background: linear-gradient(135deg, var(--gold), var(--gold-dim));
+    background: linear-gradient(135deg, var(--primary), var(--primary-dim));
     border-radius: var(--radius);
     display: inline-flex;
     align-items: center;
@@ -1421,7 +1418,7 @@ select option { background: var(--surface-2); }
 .login-submit {
     width: 100%;
     padding: 12px;
-    background: var(--gold);
+    background: var(--primary);
     color: #000;
     border: none;
     border-radius: var(--radius-sm);
@@ -1431,7 +1428,7 @@ select option { background: var(--surface-2); }
     transition: all var(--transition);
     margin-top: 4px;
 }
-.login-submit:hover { background: var(--gold-dim); transform: translateY(-1px); }
+.login-submit:hover { background: var(--primary-dim); transform: translateY(-1px); }
 
 .login-error {
     color: var(--danger);
@@ -1803,7 +1800,7 @@ select option { background: var(--surface-2); }
 .spinner {
     width: 28px; height: 28px;
     border: 3px solid var(--border);
-    border-top-color: var(--gold);
+    border-top-color: var(--primary);
     border-radius: 50%;
     animation: spin 0.8s linear infinite;
     margin: 40px auto;
